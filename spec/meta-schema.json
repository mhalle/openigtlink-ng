{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openigtlink-ng.org/spec/meta-schema.json",
  "$comment": "GENERATED from the Pydantic models in corpus-tools/src/oigtl_corpus_tools/schema/ (types.py, element.py, field.py, message.py) \u2014 do not edit by hand; run 'uv run oigtl-corpus schema emit-meta' to regenerate.",
  "title": "OpenIGTLink Message Schema",
  "description": "Meta-schema that validates the shape of every file under spec/schemas/. Each message-type schema describes one OpenIGTLink wire message and is consumed by codegen tools across all language ports.",
  "$defs": {
    "CountSource": {
      "description": "How an array's element count is derived when not given explicitly.\n\n``remaining`` means the count is computed at parse time as the number\nof elements that fit in the body bytes not consumed by earlier\nfields: ``count = (body_size - bytes_consumed_so_far) / element_size``.\nThe division must be exact \u2014 a message whose body_size does not divide\nevenly is malformed. This matches the dominant pattern across the\nOpenIGTLink message catalog (CAPABILITY, POINT, TDATA, QTDATA,\nTRAJECTORY, IMGMETA, LBMETA), where the element count is implicit in\nthe body size rather than carried in a header field.\n\nThis enum is reserved for future growth (e.g. an NDARRAY-style\nproduct-of-sizes variant); today it has a single value.",
      "enum": [
        "remaining"
      ],
      "title": "CountSource",
      "type": "string"
    },
    "ElementDescriptor": {
      "additionalProperties": false,
      "description": "Inline description of an array's element type.\n\nUsed when an array / struct_array field has elements that are more\ncomplex than a single primitive but simpler than a full referenced\nstruct \u2014 for example, CAPABILITY's array of 12-byte null-padded\nASCII strings, where each element is a ``fixed_string`` with\n``size_bytes: 12``.\n\nAn ElementDescriptor carries only the *wire-shape* attributes\n(``type``, ``size_bytes``, ``encoding``, etc.) \u2014 not field-level\nattributes like ``name`` or top-level attributes like ``count``,\nsince it describes what a single element looks like, not a field in\nits own right. Descriptions and notes are optional: an element is\ntypically documented at its parent field's description.",
      "properties": {
        "type": {
          "anyOf": [
            {
              "$ref": "#/$defs/PrimitiveType"
            },
            {
              "$ref": "#/$defs/FieldType"
            }
          ],
          "description": "Wire type of the element. Primitives are the common case; ``fixed_string`` / ``fixed_bytes`` let an array carry uniformly-sized structured elements.",
          "title": "Type"
        },
        "size_bytes": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "const": "variable",
              "type": "string"
            }
          ],
          "default": null,
          "description": "Element byte size. Required when ``type`` is ``fixed_string`` or ``fixed_bytes``.",
          "title": "Size Bytes"
        },
        "encoding": {
          "$ref": "#/$defs/Encoding",
          "default": null,
          "description": "For string-typed elements: the expected text encoding."
        },
        "endianness": {
          "$ref": "#/$defs/Endianness",
          "default": null,
          "description": "Byte order. Defaults to big-endian (OpenIGTLink convention)."
        },
        "null_padded": {
          "default": null,
          "description": "For ``fixed_string`` elements: whether values shorter than ``size_bytes`` are null-padded. Defaults to true.",
          "title": "Null Padded",
          "type": "boolean"
        },
        "length_prefix_type": {
          "$ref": "#/$defs/PrimitiveType",
          "default": null,
          "description": "For ``length_prefixed_string`` elements: the integer type of the length prefix."
        },
        "description": {
          "default": null,
          "description": "Optional. Elements are normally documented via the parent field's description; use this only when per-element docs would add clarity.",
          "title": "Description",
          "type": "string"
        },
        "fields": {
          "default": null,
          "description": "For ``struct`` elements: ordered list of named sub-fields. Each sub-field is a full FieldSchema (primitive, array, or nested struct). Required when ``type`` is ``struct`` and MUST NOT be set for other types.",
          "items": {
            "$ref": "#/$defs/FieldSchema"
          },
          "title": "Fields",
          "type": "array"
        }
      },
      "required": [
        "type"
      ],
      "title": "ElementDescriptor",
      "type": "object"
    },
    "Encoding": {
      "description": "Text encoding for string-typed fields.",
      "enum": [
        "ascii",
        "utf-8",
        "binary"
      ],
      "title": "Encoding",
      "type": "string"
    },
    "Endianness": {
      "description": "Byte order for a multi-byte field on the wire.",
      "enum": [
        "big",
        "little"
      ],
      "title": "Endianness",
      "type": "string"
    },
    "ExtendedHeader": {
      "additionalProperties": false,
      "description": "V3 extended-header layout, if non-default for a message type.",
      "properties": {
        "required": {
          "default": null,
          "description": "Whether the v3 extended header is required for this message type.",
          "title": "Required",
          "type": "boolean"
        },
        "fields": {
          "default": null,
          "description": "Extended-header field layout, if non-default.",
          "items": {
            "$ref": "#/$defs/FieldSchema"
          },
          "title": "Fields",
          "type": "array"
        }
      },
      "title": "ExtendedHeader",
      "type": "object"
    },
    "FieldSchema": {
      "additionalProperties": false,
      "description": "One field in a message body.",
      "properties": {
        "name": {
          "description": "Field name, snake_case. Used as-is in codegen; language ports may apply idiomatic casing conversions.",
          "pattern": "^[a-z][a-z0-9_]*$",
          "title": "Name",
          "type": "string"
        },
        "type": {
          "anyOf": [
            {
              "$ref": "#/$defs/PrimitiveType"
            },
            {
              "$ref": "#/$defs/FieldType"
            }
          ],
          "description": "Field type. Primitives are encoded in the order declared. Composite types require additional attributes (``size_bytes``, ``count``, ``element_type``, ``length_prefix_type``, ``encoding``, etc.). ``trailing_string`` consumes all remaining body bytes and MUST be the last field in its message.",
          "title": "Type"
        },
        "description": {
          "description": "Concise description of the field's meaning. Required.",
          "minLength": 1,
          "title": "Description",
          "type": "string"
        },
        "rationale": {
          "default": null,
          "description": "Optional. Why this field is shaped this way.",
          "title": "Rationale",
          "type": "string"
        },
        "introduced_in": {
          "$ref": "#/$defs/ProtocolVersion",
          "default": null,
          "description": "Protocol version in which this field first appeared, if different from the message's ``introduced_in``."
        },
        "endianness": {
          "$ref": "#/$defs/Endianness",
          "default": null,
          "description": "Byte order. Defaults to big-endian (OpenIGTLink convention). Rarely overridden; if overridden, explain in ``rationale``."
        },
        "size_bytes": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "const": "variable",
              "type": "string"
            }
          ],
          "default": null,
          "description": "Size in bytes on the wire. Required for ``fixed_string`` and ``fixed_bytes``; optional for other types if derivable from ``type`` alone.",
          "title": "Size Bytes"
        },
        "count": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "pattern": "^[a-z][a-z0-9_]*$",
              "type": "string"
            }
          ],
          "default": null,
          "description": "For ``array`` / ``struct_array``: either a fixed integer count, or the name of a sibling field that carries the count. Mutually exclusive with ``count_from``.",
          "title": "Count"
        },
        "count_from": {
          "$ref": "#/$defs/CountSource",
          "default": null,
          "description": "For ``array`` / ``struct_array``: derive the element count implicitly rather than from a fixed integer or sibling field. Value ``remaining`` means ``count = (body_size - bytes_consumed_so_far) / element_size``, and the division must be exact. Mutually exclusive with ``count``; exactly one of the two is required on array / struct_array fields."
        },
        "element_type": {
          "anyOf": [
            {
              "$ref": "#/$defs/PrimitiveType"
            },
            {
              "$ref": "#/$defs/ElementDescriptor"
            },
            {
              "pattern": "^[A-Z][A-Z0-9_]*$",
              "type": "string"
            }
          ],
          "default": null,
          "description": "For ``array``: the element's primitive type. For ``struct_array``: the name of an inline struct (not yet supported).",
          "title": "Element Type"
        },
        "layout": {
          "default": null,
          "description": "Optional semantic-layout hint (e.g. 'column_major_3x4' for TRANSFORM's matrix). Codegen may use this for helper methods; it has no effect on wire encoding.",
          "title": "Layout",
          "type": "string"
        },
        "length_prefix_type": {
          "$ref": "#/$defs/PrimitiveType",
          "default": null,
          "description": "For ``length_prefixed_string``: the integer type used to encode the length prefix."
        },
        "encoding": {
          "$ref": "#/$defs/Encoding",
          "default": null,
          "description": "For string types: the expected text encoding."
        },
        "null_padded": {
          "default": null,
          "description": "For ``fixed_string``: whether values shorter than ``size_bytes`` are padded with null bytes (true) or the full region is treated as the string (false). Defaults to true.",
          "title": "Null Padded",
          "type": "boolean"
        },
        "null_terminated": {
          "default": null,
          "description": "For ``trailing_string``: whether the final byte on the wire MUST be 0x00. When true, the null is part of the field bytes (a non-empty string occupies ``len+1`` bytes) and the string value is the bytes excluding the final null. Defaults to false.",
          "title": "Null Terminated",
          "type": "boolean"
        },
        "legacy_notes": {
          "default": null,
          "description": "Historical quirks for this field that must be preserved for wire compatibility.",
          "items": {
            "type": "string"
          },
          "title": "Legacy Notes",
          "type": "array"
        },
        "notes": {
          "default": null,
          "description": "Other prose notes that do not fit ``rationale`` or ``legacy_notes``.",
          "items": {
            "type": "string"
          },
          "title": "Notes",
          "type": "array"
        }
      },
      "required": [
        "name",
        "type",
        "description"
      ],
      "title": "FieldSchema",
      "type": "object"
    },
    "FieldType": {
      "description": "Composite field types \u2014 everything that isn't a numeric primitive.\n\n- ``fixed_string`` requires ``size_bytes``.\n- ``length_prefixed_string`` requires a ``length_prefix_type``.\n- ``trailing_string`` consumes all remaining body bytes; MUST be the\n  last field in its message.\n- ``fixed_bytes`` requires ``size_bytes``.\n- ``array`` holds a variable number of elements of a single type;\n  element count is given by ``count`` or ``count_from``.\n- ``struct_array`` \u2014 historical alias for ``array`` with a struct\n  element; kept for symmetry with upstream terminology.\n- ``struct`` \u2014 composite element with a named ``fields`` list. Used\n  as an array element to describe per-entry layouts like POINT's\n  136-byte element or TDATA's 70-byte element.",
      "enum": [
        "fixed_string",
        "length_prefixed_string",
        "trailing_string",
        "fixed_bytes",
        "array",
        "struct_array",
        "struct"
      ],
      "title": "FieldType",
      "type": "string"
    },
    "PrimitiveType": {
      "description": "Fixed-width numeric types that can appear directly in a message body.",
      "enum": [
        "uint8",
        "uint16",
        "uint32",
        "uint64",
        "int8",
        "int16",
        "int32",
        "int64",
        "float32",
        "float64"
      ],
      "title": "PrimitiveType",
      "type": "string"
    },
    "ProtocolVersion": {
      "description": "OpenIGTLink protocol version in which a feature first appeared.",
      "enum": [
        "v1",
        "v2",
        "v3",
        "v4"
      ],
      "title": "ProtocolVersion",
      "type": "string"
    },
    "SpecReference": {
      "additionalProperties": false,
      "description": "Back-reference from a schema or field into the prose spec document.",
      "properties": {
        "section": {
          "description": "Section identifier in the prose spec document (e.g. '4.3.2' or 'Body (STATUS)').",
          "title": "Section",
          "type": "string"
        },
        "document": {
          "default": null,
          "description": "Prose spec document this entry references (e.g. 'protocol/v3.md'). Defaults to the document matching ``introduced_in``.",
          "title": "Document",
          "type": "string"
        },
        "url": {
          "default": null,
          "description": "Optional canonical URL for the spec section.",
          "format": "uri",
          "title": "Url",
          "type": "string"
        }
      },
      "required": [
        "section"
      ],
      "title": "SpecReference",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "message_type": {
      "description": "Canonical logical name for this message type (e.g. 'TRANSFORM'). Used to construct language-native type names during codegen.",
      "pattern": "^[A-Z][A-Z0-9_]*$",
      "title": "Message Type",
      "type": "string"
    },
    "type_id": {
      "description": "Wire type identifier. Written as a 12-byte ASCII field, null-padded on the right. The string value here contains the significant bytes only; padding is applied during encoding.",
      "maxLength": 12,
      "minLength": 1,
      "pattern": "^[A-Z0-9_]+$",
      "title": "Type Id",
      "type": "string"
    },
    "introduced_in": {
      "$ref": "#/$defs/ProtocolVersion",
      "description": "Protocol version in which this message type was first defined."
    },
    "deprecated_in": {
      "default": null,
      "description": "Protocol version in which this message type was deprecated, if any. Deprecated types must still round-trip correctly for wire compatibility.",
      "enum": [
        "v2",
        "v3",
        "v4"
      ],
      "title": "Deprecated In",
      "type": "string"
    },
    "description": {
      "description": "Concise human-readable summary. Required. Flows into every language port's generated API documentation.",
      "minLength": 1,
      "title": "Description",
      "type": "string"
    },
    "rationale": {
      "default": null,
      "description": "Optional prose explaining why the message type or its wire layout is shaped this way. Historical or design-decision context, not behavioral contract.",
      "title": "Rationale",
      "type": "string"
    },
    "spec_reference": {
      "$ref": "#/$defs/SpecReference",
      "default": null,
      "description": "Back-reference into the prose spec document."
    },
    "body_size": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "const": "variable",
          "type": "string"
        }
      ],
      "description": "Total body size in bytes if fixed, or the string 'variable' if determined by the fields at encode time.",
      "title": "Body Size"
    },
    "fields": {
      "description": "Ordered list of fields in the message body. Fields are parsed in order.",
      "items": {
        "$ref": "#/$defs/FieldSchema"
      },
      "title": "Fields",
      "type": "array"
    },
    "extended_header": {
      "$ref": "#/$defs/ExtendedHeader",
      "default": null,
      "description": "V3 extended-header layout for this message type, if non-default."
    },
    "metadata_allowed": {
      "default": null,
      "description": "Whether this message type accepts a trailing metadata block in v2/v3. Defaults to true.",
      "title": "Metadata Allowed",
      "type": "boolean"
    },
    "legacy_notes": {
      "default": null,
      "description": "Historical quirks that must be preserved for wire compatibility. These are contract, not documentation \u2014 every entry represents a decision that a new implementation must match. Prefix with a version tag where relevant (e.g. 'v2.1:').",
      "items": {
        "type": "string"
      },
      "title": "Legacy Notes",
      "type": "array"
    },
    "notes": {
      "default": null,
      "description": "Other prose notes that do not fit rationale, spec_reference, or legacy_notes.",
      "items": {
        "type": "string"
      },
      "title": "Notes",
      "type": "array"
    },
    "see_also": {
      "default": null,
      "description": "Logical names of related message types.",
      "items": {
        "pattern": "^[A-Z][A-Z0-9_]*$",
        "type": "string"
      },
      "title": "See Also",
      "type": "array"
    }
  },
  "required": [
    "message_type",
    "type_id",
    "introduced_in",
    "description",
    "body_size",
    "fields"
  ],
  "type": "object"
}

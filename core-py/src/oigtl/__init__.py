"""Typed Python codec for the OpenIGTLink protocol.

Top-level surface re-exports the runtime + message API:

- :mod:`oigtl.runtime` — header, CRC, framing, oracle, dispatch
- :mod:`oigtl.messages` — generated typed message classes
"""

__version__ = "0.1.0"

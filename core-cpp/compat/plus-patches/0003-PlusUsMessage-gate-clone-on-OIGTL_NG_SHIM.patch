diff --git a/src/PlusOpenIGTLink/igtlPlusUsMessage.cxx b/src/PlusOpenIGTLink/igtlPlusUsMessage.cxx
index 71e2a5a..c1dc88b 100644
--- a/src/PlusOpenIGTLink/igtlPlusUsMessage.cxx
+++ b/src/PlusOpenIGTLink/igtlPlusUsMessage.cxx
@@ -7,6 +7,7 @@ See License.txt for details.
 #include "PlusConfigure.h"
 #include "igsioTrackedFrame.h"
 #include "igtlPlusUsMessage.h"
+#include "igtlMacro.h"        // OIGTL_NG_SHIM feature-test macro (shim only)
 #include "igtl_image.h"
 #include "igtl_header.h"
 #include "igtl_util.h"
@@ -68,22 +69,32 @@ namespace igtl
 
     igtl::PlusUsMessage::Pointer msg = dynamic_cast<igtl::PlusUsMessage*>(clone.GetPointer());
 
-    int bodySize = this->m_MessageSize - IGTL_HEADER_SIZE;
-    msg->InitBuffer();
-    msg->CopyHeader(this);
-    msg->AllocateBuffer(bodySize);
+    msg->CloneReceivedStateFrom(*this);
+
+    return clone;
+  }
+
+  //----------------------------------------------------------------------------
+  void PlusUsMessage::CloneReceivedStateFrom(const PlusUsMessage& src)
+  {
+#ifdef OIGTL_NG_SHIM
+    this->CopyReceivedFrom(src);
+#else
+    int bodySize = src.m_MessageSize - IGTL_HEADER_SIZE;
+    this->InitBuffer();
+    this->CopyHeader(&src);
+    this->AllocateBuffer(bodySize);
     if (bodySize > 0)
     {
-      msg->CopyBody(this);
+      this->CopyBody(&src);
     }
 
-#if OpenIGTLink_HEADER_VERSION >= 2
-    msg->m_MetaDataHeader = this->m_MetaDataHeader;
-    msg->m_MetaDataMap = this->m_MetaDataMap;
-    msg->m_IsExtendedHeaderUnpacked = this->m_IsExtendedHeaderUnpacked;
+  #if OpenIGTLink_HEADER_VERSION >= 2
+    this->m_MetaDataHeader = src.m_MetaDataHeader;
+    this->m_MetaDataMap = src.m_MetaDataMap;
+    this->m_IsExtendedHeaderUnpacked = src.m_IsExtendedHeaderUnpacked;
+  #endif
 #endif
-
-    return clone;
   }
 
   //----------------------------------------------------------------------------
diff --git a/src/PlusOpenIGTLink/igtlPlusUsMessage.h b/src/PlusOpenIGTLink/igtlPlusUsMessage.h
index 96d70c1..80610a5 100644
--- a/src/PlusOpenIGTLink/igtlPlusUsMessage.h
+++ b/src/PlusOpenIGTLink/igtlPlusUsMessage.h
@@ -65,6 +65,11 @@ namespace igtl
     virtual int PackContent();
     virtual int UnpackContent();
 
+    // Backend-agnostic clone helper — body is ifdef'd to use the
+    // sanctioned tier-2 API on the hardened openigtlink-ng shim
+    // (OIGTL_NG_SHIM) and the original upstream idiom otherwise.
+    void CloneReceivedStateFrom(const PlusUsMessage& src);
+
     PlusUsMessage();
     ~PlusUsMessage();
 
